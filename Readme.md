# pdf-search-regex

simple pdf search with regex


### run:
- returns a promise

```shell
SearchPdf(<filename>, <searchTerm>)
  	.then(out => {
	 console.log(out);
	})
	.catch(err =>{
	 console.log(err);
	});
```
### pre-prod.



### deps

#### Requires Java

- Uses PDFBox licensed under Apache